# AGENTS.md
 
## Setup commands
- Install deps: `bun install`
- Run with `just crawl` or `just serve`
- Format and lint with `just check`
- Run tsc with `bunx tsc`

## Code style
- TypeScript strict mode
- Use functional patterns where possible
